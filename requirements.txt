# Cairn's core has zero dependencies — it's pure standard library.
# These are only needed for running tests or the example pipelines.

pytest>=8.0.0
ruff>=0.4.0

# examples/ (optional)
# scikit-learn>=1.3.0
# numpy>=1.24.0
